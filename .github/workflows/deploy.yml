# ==========================================================
# AWS CloudFormation DevOps Lab
# GitHub Actions Deployment Workflow
# ==========================================================
#
# File:
# .github/workflows/deploy.yml
#
# Purpose:
# ----------------------------------------------------------
# This workflow automatically validates and deploys the
# CloudFormation infrastructure defined in:
#
# templates/lab01.yaml
#
# The workflow performs the following tasks:
#
# 1. Checkout GitHub repository
# 2. Configure AWS credentials
# 3. Verify AWS CLI and AWS account
# 4. Check required deployment values
# 5. Create/update the template bucket stack
# 6. Get the template S3 bucket name
# 7. Upload nested CloudFormation templates to S3
# 8. Display repository information
# 9. Verify CloudFormation templates exist
# 10. Validate the main CloudFormation template
# 11. Check whether the main stack exists
# 12. Display existing stack status
# 13. Create the main stack if it does not exist
# 14. Wait for stack creation
# 15. Update the main stack if it already exists
# 16. Wait for stack update when required
# 17. Display final stack information
# 18. Display stack resources
# 19. Display stack outputs
# 20. Display deployment completion message
#
# ==========================================================


# ==========================================================
# WORKFLOW NAME
# ==========================================================

name: AWS CloudFormation Deployment


# ==========================================================
# WORKFLOW TRIGGERS
# ==========================================================
#
# The workflow runs:
#
# 1. Automatically when code is pushed to main
# 2. Manually from GitHub Actions
#
# ==========================================================

on:

  push:
    branches:
      - main

  workflow_dispatch:


# ==========================================================
# GLOBAL ENVIRONMENT VARIABLES
# ==========================================================

env:

  # --------------------------------------------------------
  # AWS Region
  # --------------------------------------------------------

  AWS_REGION: us-east-1


  # --------------------------------------------------------
  # Main CloudFormation Stack
  # --------------------------------------------------------

  STACK_NAME: Lab01-CloudFormation


  # --------------------------------------------------------
  # EC2 Key Pair
  #
  # IMPORTANT:
  # This must exactly match an existing EC2 Key Pair
  # in the AWS region above.
  #
  # Example:
  #
  # Lab-KeyPair
  #
  # --------------------------------------------------------

  KEY_PAIR_NAME: Lab-KeyPair


  # --------------------------------------------------------
  # Template Bucket CloudFormation Stack
  #
  # This stack creates the S3 bucket used to store
  # nested CloudFormation templates.
  # --------------------------------------------------------

  TEMPLATE_BUCKET_STACK: Lab01-CloudFormation-TemplateBucket


  # --------------------------------------------------------
  # Template Bucket CloudFormation Template
  # --------------------------------------------------------

  TEMPLATE_BUCKET_FILE: templates/TemplateBucket-MainStack.yaml


  # --------------------------------------------------------
  # Main CloudFormation Template
  # --------------------------------------------------------

  TEMPLATE_FILE: templates/lab01.yaml


# ==========================================================
# JOBS
# ==========================================================

jobs:

  deploy:

    # --------------------------------------------------------
    # GitHub-hosted Linux runner
    # --------------------------------------------------------

    runs-on: ubuntu-latest

    name: Validate and Deploy CloudFormation Stack


    # ========================================================
    # STEPS
    # ========================================================

    steps:


      # ======================================================
      # STEP 1
      # Checkout Repository
      # ======================================================
      #
      # Downloads the repository onto the GitHub Actions
      # runner.
      #
      # ======================================================

      - name: Checkout Repository

        uses: actions/checkout@v4


      # ======================================================
      # STEP 2
      # Configure AWS Credentials
      # ======================================================
      #
      # Required GitHub Repository Secrets:
      #
      # AWS_ACCESS_KEY_ID
      # AWS_SECRET_ACCESS_KEY
      # DB_MASTER_PASSWORD
      #
      # IMPORTANT:
      #
      # Never print the actual DB password.
      #
      # ======================================================

      - name: Configure AWS Credentials

        uses: aws-actions/configure-aws-credentials@v4

        with:

          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}

          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

          aws-region: ${{ env.AWS_REGION }}


      # ======================================================
      # STEP 3
      # Verify AWS CLI and AWS Account
      # ======================================================

      - name: Verify AWS CLI

        shell: bash

        run: |

          echo "======================================"
          echo "AWS CLI Version"
          echo "======================================"

          aws --version

          echo ""

          echo "======================================"
          echo "AWS Account Information"
          echo "======================================"

          aws sts get-caller-identity \
            --no-cli-pager


      # ======================================================
      # STEP 4
      # Check Required Deployment Values
      # ======================================================
      #
      # The database password is checked but NEVER displayed.
      #
      # ======================================================

      - name: Check Required Deployment Values

        shell: bash

        env:

          DB_MASTER_PASSWORD: ${{ secrets.DB_MASTER_PASSWORD }}

        run: |

          echo "======================================"
          echo "Checking Required Deployment Values"
          echo "======================================"

          echo "AWS Region:"
          echo "$AWS_REGION"

          echo ""

          echo "Main CloudFormation Stack:"
          echo "$STACK_NAME"

          echo ""

          echo "Main Template:"
          echo "$TEMPLATE_FILE"

          echo ""

          echo "Template Bucket Stack:"
          echo "$TEMPLATE_BUCKET_STACK"

          echo ""

          echo "Template Bucket Template:"
          echo "$TEMPLATE_BUCKET_FILE"

          echo ""

          echo "EC2 Key Pair:"
          echo "$KEY_PAIR_NAME"

          echo ""


          # --------------------------------------------------
          # Check Key Pair
          # --------------------------------------------------

          if [ -z "$KEY_PAIR_NAME" ]; then

            echo "ERROR: KEY_PAIR_NAME is empty."

            exit 1

          fi


          # --------------------------------------------------
          # Check DB Password
          # --------------------------------------------------

          if [ -z "$DB_MASTER_PASSWORD" ]; then

            echo "ERROR: DB_MASTER_PASSWORD GitHub Secret is empty or unavailable."

            echo ""
            echo "Create this GitHub repository secret:"
            echo "DB_MASTER_PASSWORD"

            exit 1

          fi


          # --------------------------------------------------
          # Basic password length validation
          # --------------------------------------------------

          if [ "${#DB_MASTER_PASSWORD}" -lt 8 ]; then

            echo "ERROR: DB_MASTER_PASSWORD must contain at least 8 characters."

            exit 1

          fi


          echo "KEY_PAIR_NAME is available."

          echo "DB_MASTER_PASSWORD is available."

          echo "DB_MASTER_PASSWORD length: ${#DB_MASTER_PASSWORD}"

          echo ""

          echo "All required deployment values are available."


      # ======================================================
      # STEP 5
      # Create or Update Template Bucket Stack
      # ======================================================
      #
      # This stack creates the S3 bucket used to store
      # nested CloudFormation templates.
      #
      # ======================================================

      - name: Create or Update Template Bucket Stack

        shell: bash

        run: |

          echo "======================================"
          echo "Template Bucket CloudFormation Stack"
          echo "======================================"

          echo "Stack Name:"
          echo "$TEMPLATE_BUCKET_STACK"

          echo ""

          echo "Template:"
          echo "$TEMPLATE_BUCKET_FILE"

          echo ""


          # --------------------------------------------------
          # Verify template exists
          # --------------------------------------------------

          if [ ! -f "$TEMPLATE_BUCKET_FILE" ]; then

            echo "ERROR: Template bucket template was not found:"
            echo "$TEMPLATE_BUCKET_FILE"

            exit 1

          fi


          # --------------------------------------------------
          # Validate template
          # --------------------------------------------------

          echo "Validating template bucket CloudFormation template..."

          aws cloudformation validate-template \
            --template-body "file://$TEMPLATE_BUCKET_FILE" \
            --region "$AWS_REGION" \
            --no-cli-pager


          echo ""

          echo "Template bucket template validation completed."


          # --------------------------------------------------
          # Check whether stack exists
          # --------------------------------------------------

          if aws cloudformation describe-stacks \
            --stack-name "$TEMPLATE_BUCKET_STACK" \
            --region "$AWS_REGION" \
            --no-cli-pager \
            > /dev/null 2>&1
          then

            echo ""
            echo "Template bucket stack already exists."


            # ------------------------------------------------
            # Get current status
            # ------------------------------------------------

            TEMPLATE_BUCKET_STATUS=$(aws cloudformation describe-stacks \
              --stack-name "$TEMPLATE_BUCKET_STACK" \
              --region "$AWS_REGION" \
              --query "Stacks[0].StackStatus" \
              --output text \
              --no-cli-pager)


            echo ""
            echo "Current Template Bucket Stack Status:"
            echo "$TEMPLATE_BUCKET_STATUS"


            # ------------------------------------------------
            # Prevent update while stack is busy
            # ------------------------------------------------

            case "$TEMPLATE_BUCKET_STATUS" in

              CREATE_IN_PROGRESS|UPDATE_IN_PROGRESS|UPDATE_COMPLETE_CLEANUP_IN_PROGRESS|DELETE_IN_PROGRESS)

                echo ""
                echo "ERROR: Template bucket stack is currently busy."
                echo "Current status: $TEMPLATE_BUCKET_STATUS"

                exit 1

                ;;

              ROLLBACK_COMPLETE|ROLLBACK_FAILED|UPDATE_ROLLBACK_FAILED|CREATE_FAILED)

                echo ""
                echo "ERROR: Template bucket stack is in a failed state."
                echo "Current status: $TEMPLATE_BUCKET_STATUS"

                echo ""
                echo "Manual recovery may be required."

                exit 1

                ;;

            esac


            # ------------------------------------------------
            # Update existing stack
            # ------------------------------------------------

            echo ""
            echo "Checking for template bucket stack updates..."


            rm -f /tmp/template-bucket-update.txt


            set +e

            aws cloudformation update-stack \
              --stack-name "$TEMPLATE_BUCKET_STACK" \
              --template-body "file://$TEMPLATE_BUCKET_FILE" \
              --region "$AWS_REGION" \
              --no-cli-pager \
              2>&1 | tee /tmp/template-bucket-update.txt

            UPDATE_EXIT_CODE=${PIPESTATUS[0]}

            set -e


            # ------------------------------------------------
            # Update started successfully
            # ------------------------------------------------

            if [ "$UPDATE_EXIT_CODE" -eq 0 ]; then

              echo ""
              echo "Template bucket stack update started."

              echo ""
              echo "Waiting for template bucket stack update..."

              aws cloudformation wait stack-update-complete \
                --stack-name "$TEMPLATE_BUCKET_STACK" \
                --region "$AWS_REGION"

              echo ""
              echo "Template bucket stack update completed."


            # ------------------------------------------------
            # No changes
            # ------------------------------------------------

            elif grep -qi "No updates are to be performed" \
              /tmp/template-bucket-update.txt
            then

              echo ""
              echo "No template bucket changes detected."

              echo "Existing template bucket will be used."


            # ------------------------------------------------
            # Real error
            # ------------------------------------------------

            else

              echo ""
              echo "ERROR: Template bucket stack update failed."

              echo ""
              echo "AWS CloudFormation Error:"

              cat /tmp/template-bucket-update.txt

              exit "$UPDATE_EXIT_CODE"

            fi


          else

            # ------------------------------------------------
            # Stack does not exist
            # ------------------------------------------------

            echo ""
            echo "Template bucket stack does not exist."

            echo ""
            echo "Creating template bucket stack..."


            aws cloudformation create-stack \
              --stack-name "$TEMPLATE_BUCKET_STACK" \
              --template-body "file://$TEMPLATE_BUCKET_FILE" \
              --region "$AWS_REGION" \
              --no-cli-pager


            echo ""

            echo "Template bucket stack creation started."


            echo ""
            echo "Waiting for template bucket stack creation..."


            aws cloudformation wait stack-create-complete \
              --stack-name "$TEMPLATE_BUCKET_STACK" \
              --region "$AWS_REGION"


            echo ""

            echo "Template bucket stack creation completed."

          fi


          echo ""
          echo "======================================"
          echo "Template Bucket Stack Ready"
          echo "======================================"


      # ======================================================
      # STEP 6
      # Get Template S3 Bucket Name
      # ======================================================

      - name: Get Template Bucket Name

        shell: bash

        run: |

          echo "======================================"
          echo "Getting Template S3 Bucket Name"
          echo "======================================"


          TEMPLATE_BUCKET=$(aws cloudformation describe-stacks \
            --stack-name "$TEMPLATE_BUCKET_STACK" \
            --region "$AWS_REGION" \
            --query "Stacks[0].Outputs[?OutputKey=='BucketName'].OutputValue" \
            --output text \
            --no-cli-pager)


          # --------------------------------------------------
          # Verify bucket name
          # --------------------------------------------------

          if [ -z "$TEMPLATE_BUCKET" ] || [ "$TEMPLATE_BUCKET" = "None" ]; then

            echo "ERROR: Could not retrieve S3 template bucket name."

            echo ""
            echo "Verify that TemplateBucket-MainStack.yaml contains:"
            echo ""
            echo "Outputs:"
            echo "  BucketName:"
            echo "    Value: !Ref <BucketResource>"

            exit 1

          fi


          # --------------------------------------------------
          # Save bucket name for later workflow steps
          # --------------------------------------------------

          echo "TEMPLATE_BUCKET=$TEMPLATE_BUCKET" >> "$GITHUB_ENV"


          echo ""

          echo "Template S3 Bucket:"
          echo "$TEMPLATE_BUCKET"


      # ======================================================
      # STEP 7
      # Upload CloudFormation Templates to S3
      # ======================================================
      #
      # The main and nested CloudFormation templates are
      # uploaded to the template bucket.
      #
      # ======================================================

      - name: Upload Nested Templates to S3

        shell: bash

        run: |

          echo "======================================"
          echo "Uploading CloudFormation Templates"
          echo "======================================"

          echo "S3 Bucket:"
          echo "$TEMPLATE_BUCKET"

          echo ""

          echo "Source Directory:"
          echo "templates/"

          echo ""


          # --------------------------------------------------
          # Upload templates.
          #
          # Exclude the template responsible for creating
          # the template bucket itself.
          # --------------------------------------------------

          aws s3 sync \
            templates/ \
            "s3://$TEMPLATE_BUCKET/templates/" \
            --exclude "TemplateBucket-MainStack.yaml" \
            --region "$AWS_REGION"


          echo ""

          echo "CloudFormation templates uploaded successfully."


          echo ""
          echo "S3 Template Files:"


          aws s3 ls \
            "s3://$TEMPLATE_BUCKET/templates/" \
            --recursive \
            --region "$AWS_REGION"


      # ======================================================
      # STEP 8
      # Display GitHub Runner Environment
      # ======================================================

      - name: Display Environment

        shell: bash

        run: |

          echo "======================================"
          echo "GitHub Runner Information"
          echo "======================================"

          echo "Current Directory:"
          pwd

          echo ""

          echo "Repository Files:"
          ls -la

          echo ""

          echo "CloudFormation Template:"
          echo "$TEMPLATE_FILE"

          echo ""

          echo "AWS Region:"
          echo "$AWS_REGION"

          echo ""

          echo "CloudFormation Stack:"
          echo "$STACK_NAME"

          echo ""

          echo "Template Bucket Stack:"
          echo "$TEMPLATE_BUCKET_STACK"

          echo ""

          echo "Template Bucket:"
          echo "$TEMPLATE_BUCKET"


      # ======================================================
      # STEP 9
      # Verify CloudFormation Templates Exist
      # ======================================================

      - name: Check CloudFormation Templates

        shell: bash

        run: |

          echo "======================================"
          echo "Checking CloudFormation Templates"
          echo "======================================"


          # --------------------------------------------------
          # Check Template Bucket template
          # --------------------------------------------------

          if [ ! -f "$TEMPLATE_BUCKET_FILE" ]; then

            echo "ERROR: Template bucket template not found:"
            echo "$TEMPLATE_BUCKET_FILE"

            exit 1

          fi


          echo "Template bucket template found:"
          echo "$TEMPLATE_BUCKET_FILE"


          echo ""


          # --------------------------------------------------
          # Check Main template
          # --------------------------------------------------

          if [ ! -f "$TEMPLATE_FILE" ]; then

            echo "ERROR: Main CloudFormation template not found:"
            echo "$TEMPLATE_FILE"

            exit 1

          fi


          echo "Main CloudFormation template found:"
          echo "$TEMPLATE_FILE"


      # ======================================================
      # STEP 10
      # Validate Main CloudFormation Template
      # ======================================================

      - name: Validate CloudFormation Template

        shell: bash

        run: |

          echo "======================================"
          echo "Validating CloudFormation Template"
          echo "======================================"


          aws cloudformation validate-template \
            --template-body "file://$TEMPLATE_FILE" \
            --region "$AWS_REGION" \
            --no-cli-pager


          echo ""

          echo "CloudFormation template validation completed successfully."


      # ======================================================
      # STEP 11
      # Check Whether Main Stack Exists
      # ======================================================

      - name: Check Stack Status

        id: stack

        shell: bash

        run: |

          echo "======================================"
          echo "Checking CloudFormation Stack"
          echo "======================================"


          if aws cloudformation describe-stacks \
            --stack-name "$STACK_NAME" \
            --region "$AWS_REGION" \
            --no-cli-pager \
            > /dev/null 2>&1
          then

            echo "CloudFormation stack already exists."

            echo "exists=true" >> "$GITHUB_OUTPUT"

          else

            echo "CloudFormation stack does not exist."

            echo "exists=false" >> "$GITHUB_OUTPUT"

          fi


      # ======================================================
      # STEP 12
      # Display Existing Stack Status
      # ======================================================

      - name: Display Existing Stack Status

        if: steps.stack.outputs.exists == 'true'

        shell: bash

        run: |

          echo "======================================"
          echo "Existing Stack Status"
          echo "======================================"


          STACK_STATUS=$(aws cloudformation describe-stacks \
            --stack-name "$STACK_NAME" \
            --region "$AWS_REGION" \
            --query "Stacks[0].StackStatus" \
            --output text \
            --no-cli-pager)


          echo "Current Stack Status:"
          echo "$STACK_STATUS"


          # --------------------------------------------------
          # Detect unrecoverable states
          # --------------------------------------------------

          case "$STACK_STATUS" in

            ROLLBACK_COMPLETE|ROLLBACK_FAILED|CREATE_FAILED)

              echo ""
              echo "ERROR: Main stack is in a failed state:"
              echo "$STACK_STATUS"

              echo ""
              echo "If this is ROLLBACK_COMPLETE, delete the failed"
              echo "stack and run the GitHub Actions workflow again."

              exit 1

              ;;

            UPDATE_ROLLBACK_FAILED)

              echo ""
              echo "ERROR: Main stack is in UPDATE_ROLLBACK_FAILED."

              echo ""
              echo "CloudFormation rollback recovery is required"
              echo "before another update can be performed."

              exit 1

              ;;

          esac


      # ======================================================
      # STEP 13
      # Debug Deployment Environment
      # ======================================================

      - name: Debug Deployment Environment

        shell: bash

        env:

          DB_MASTER_PASSWORD: ${{ secrets.DB_MASTER_PASSWORD }}

        run: |

          echo "======================================"
          echo "DEPLOYMENT DEBUG"
          echo "======================================"

          echo "AWS_REGION=$AWS_REGION"

          echo "STACK_NAME=$STACK_NAME"

          echo "KEY_PAIR_NAME=$KEY_PAIR_NAME"

          echo "TEMPLATE_FILE=$TEMPLATE_FILE"

          echo "TEMPLATE_BUCKET=$TEMPLATE_BUCKET"

          echo ""

          if [ -z "$KEY_PAIR_NAME" ]; then

            echo "KEY_PAIR_NAME: EMPTY"

          else

            echo "KEY_PAIR_NAME: SET"

          fi


          if [ -z "$DB_MASTER_PASSWORD" ]; then

            echo "DB_MASTER_PASSWORD: EMPTY"

          else

            echo "DB_MASTER_PASSWORD: SET"

            echo "DB_MASTER_PASSWORD length: ${#DB_MASTER_PASSWORD}"

          fi


          echo ""

          echo "======================================"
          echo "END DEPLOYMENT DEBUG"
          echo "======================================"


      # ======================================================
      # STEP 14
      # Create New Main CloudFormation Stack
      # ======================================================
      #
      # IMPORTANT:
      #
      # The previous workflow was missing:
      #
      # DBMasterPassword
      #
      # This version explicitly passes it.
      #
      # ======================================================

      - name: Create CloudFormation Stack

        if: steps.stack.outputs.exists == 'false'

        shell: bash

        env:

          DB_MASTER_PASSWORD: ${{ secrets.DB_MASTER_PASSWORD }}

        run: |

          echo "======================================"
          echo "Creating CloudFormation Stack"
          echo "======================================"

          echo "Stack Name:"
          echo "$STACK_NAME"

          echo "Template:"
          echo "$TEMPLATE_FILE"

          echo "Region:"
          echo "$AWS_REGION"

          echo "Key Pair:"
          echo "$KEY_PAIR_NAME"

          echo "Template Bucket:"
          echo "$TEMPLATE_BUCKET"

          echo ""


          # --------------------------------------------------
          # Validate Key Pair
          # --------------------------------------------------

          if [ -z "$KEY_PAIR_NAME" ]; then

            echo "ERROR: KEY_PAIR_NAME is empty."

            exit 1

          fi


          # --------------------------------------------------
          # Validate database password
          # --------------------------------------------------

          if [ -z "$DB_MASTER_PASSWORD" ]; then

            echo "ERROR: DB_MASTER_PASSWORD is empty."

            exit 1

          fi


          if [ "${#DB_MASTER_PASSWORD}" -lt 8 ]; then

            echo "ERROR: DB_MASTER_PASSWORD must contain at least 8 characters."

            exit 1

          fi


          echo "KeyPairName is available."

          echo "DBMasterPassword is available."

          echo "DBMasterPassword length: ${#DB_MASTER_PASSWORD}"

          echo ""


          # --------------------------------------------------
          # Create CloudFormation stack
          #
          # IMPORTANT:
          #
          # Do NOT put spaces after the backslash.
          #
          # The backslash must be the final character.
          #
          # --------------------------------------------------

          echo "Starting CloudFormation stack creation..."


          aws cloudformation create-stack \
            --stack-name "$STACK_NAME" \
            --template-body "file://$TEMPLATE_FILE" \
            --parameters \
              "ParameterKey=TemplateBucketName,ParameterValue=$TEMPLATE_BUCKET" \
              "ParameterKey=KeyPairName,ParameterValue=$KEY_PAIR_NAME" \
              "ParameterKey=DBMasterPassword,ParameterValue=$DB_MASTER_PASSWORD" \
            --capabilities CAPABILITY_NAMED_IAM CAPABILITY_AUTO_EXPAND \
            --region "$AWS_REGION" \
            --no-cli-pager


          echo ""

          echo "Main CloudFormation stack creation started."


      # ======================================================
      # STEP 15
      # Wait for Stack Creation
      # ======================================================

      - name: Wait for Stack Creation

        if: steps.stack.outputs.exists == 'false'

        shell: bash

        run: |

          echo "======================================"
          echo "Waiting for Stack Creation"
          echo "======================================"


          aws cloudformation wait stack-create-complete \
            --stack-name "$STACK_NAME" \
            --region "$AWS_REGION"


          echo ""

          echo "Stack creation completed successfully."


      # ======================================================
      # STEP 16
      # Update Existing Main CloudFormation Stack
      # ======================================================
      #
      # IMPORTANT:
      #
      # DBMasterPassword is passed here as well.
      #
      # ======================================================

      - name: Update CloudFormation Stack

        if: steps.stack.outputs.exists == 'true'

        shell: bash

        env:

          DB_MASTER_PASSWORD: ${{ secrets.DB_MASTER_PASSWORD }}

        run: |

          echo "======================================"
          echo "Updating CloudFormation Stack"
          echo "======================================"

          echo "Stack Name:"
          echo "$STACK_NAME"

          echo "Template:"
          echo "$TEMPLATE_FILE"

          echo "Region:"
          echo "$AWS_REGION"

          echo "Key Pair:"
          echo "$KEY_PAIR_NAME"

          echo "Template Bucket:"
          echo "$TEMPLATE_BUCKET"

          echo ""


          # --------------------------------------------------
          # Validate DB password
          # --------------------------------------------------

          if [ -z "$DB_MASTER_PASSWORD" ]; then

            echo "ERROR: DB_MASTER_PASSWORD is empty."

            exit 1

          fi


          # --------------------------------------------------
          # Get current stack status
          # --------------------------------------------------

          STACK_STATUS=$(aws cloudformation describe-stacks \
            --stack-name "$STACK_NAME" \
            --region "$AWS_REGION" \
            --query "Stacks[0].StackStatus" \
            --output text \
            --no-cli-pager)


          echo "Current Stack Status:"
          echo "$STACK_STATUS"


          # --------------------------------------------------
          # Prevent updates while stack is busy
          # --------------------------------------------------

          case "$STACK_STATUS" in

            CREATE_IN_PROGRESS|UPDATE_IN_PROGRESS|UPDATE_COMPLETE_CLEANUP_IN_PROGRESS|DELETE_IN_PROGRESS)

              echo ""
              echo "ERROR: Main CloudFormation stack is currently busy."

              echo "Current status:"
              echo "$STACK_STATUS"

              exit 1

              ;;

            ROLLBACK_COMPLETE|ROLLBACK_FAILED|CREATE_FAILED)

              echo ""
              echo "ERROR: Main CloudFormation stack is in a failed state."

              echo "Current status:"
              echo "$STACK_STATUS"

              exit 1

              ;;

            UPDATE_ROLLBACK_FAILED)

              echo ""
              echo "ERROR: Main CloudFormation stack is in UPDATE_ROLLBACK_FAILED."

              echo ""
              echo "CloudFormation rollback recovery is required."

              exit 1

              ;;

          esac


          # --------------------------------------------------
          # Remove old output file
          # --------------------------------------------------

          rm -f /tmp/main-stack-update.txt


          # --------------------------------------------------
          # Execute CloudFormation update
          #
          # set +e allows us to inspect the AWS CLI response.
          # --------------------------------------------------

          set +e


          aws cloudformation update-stack \
            --stack-name "$STACK_NAME" \
            --template-body "file://$TEMPLATE_FILE" \
            --parameters \
              "ParameterKey=TemplateBucketName,ParameterValue=$TEMPLATE_BUCKET,UsePreviousValue=false" \
              "ParameterKey=KeyPairName,ParameterValue=$KEY_PAIR_NAME,UsePreviousValue=false" \
              "ParameterKey=DBMasterPassword,ParameterValue=$DB_MASTER_PASSWORD,UsePreviousValue=false" \
            --capabilities CAPABILITY_NAMED_IAM CAPABILITY_AUTO_EXPAND \
            --region "$AWS_REGION" \
            --no-cli-pager \
            2>&1 | tee /tmp/main-stack-update.txt


          UPDATE_EXIT_CODE=${PIPESTATUS[0]}


          set -e


          # --------------------------------------------------
          # Update started successfully
          # --------------------------------------------------

          if [ "$UPDATE_EXIT_CODE" -eq 0 ]; then

            echo ""
            echo "CloudFormation stack update started."

            echo "UPDATE_STARTED=true" >> "$GITHUB_ENV"


          # --------------------------------------------------
          # No changes detected
          # --------------------------------------------------

          elif grep -qi "No updates are to be performed" \
            /tmp/main-stack-update.txt
          then

            echo ""
            echo "No main stack changes detected."

            echo "Existing CloudFormation infrastructure will be used."

            echo "UPDATE_STARTED=false" >> "$GITHUB_ENV"


          # --------------------------------------------------
          # Real error
          # --------------------------------------------------

          else

            echo ""
            echo "ERROR: Main CloudFormation stack update failed."

            echo ""
            echo "AWS CloudFormation Error:"

            cat /tmp/main-stack-update.txt

            exit "$UPDATE_EXIT_CODE"

          fi


      # ======================================================
      # STEP 17
      # Wait for Existing Stack Update
      # ======================================================
      #
      # Only wait if an update was actually started.
      #
      # ======================================================

      - name: Wait for Stack Update

        if: steps.stack.outputs.exists == 'true' && env.UPDATE_STARTED == 'true'

        shell: bash

        run: |

          echo "======================================"
          echo "Waiting for Main Stack Update"
          echo "======================================"


          aws cloudformation wait stack-update-complete \
            --stack-name "$STACK_NAME" \
            --region "$AWS_REGION"


          echo ""

          echo "Stack update completed successfully."


      # ======================================================
      # STEP 18
      # Display Final Stack Information
      # ======================================================

      - name: Display Stack Information

        shell: bash

        run: |

          echo "======================================"
          echo "CloudFormation Stack Details"
          echo "======================================"


          aws cloudformation describe-stacks \
            --stack-name "$STACK_NAME" \
            --region "$AWS_REGION" \
            --no-cli-pager


      # ======================================================
      # STEP 19
      # Display Stack Resources
      # ======================================================

      - name: List Stack Resources

        shell: bash

        run: |

          echo "======================================"
          echo "CloudFormation Stack Resources"
          echo "======================================"


          aws cloudformation list-stack-resources \
            --stack-name "$STACK_NAME" \
            --region "$AWS_REGION" \
            --no-cli-pager


      # ======================================================
      # STEP 20
      # Display Stack Outputs
      # ======================================================

      - name: Display Stack Outputs

        shell: bash

        run: |

          echo "======================================"
          echo "CloudFormation Stack Outputs"
          echo "======================================"


          aws cloudformation describe-stacks \
            --stack-name "$STACK_NAME" \
            --region "$AWS_REGION" \
            --query "Stacks[0].Outputs" \
            --output table \
            --no-cli-pager


      # ======================================================
      # STEP 21
      # Deployment Complete
      # ======================================================

      - name: Deployment Complete

        shell: bash

        run: |

          echo ""

          echo "======================================"
          echo "CloudFormation Deployment Completed"
          echo "======================================"

          echo ""

          echo "Stack Name:"
          echo "$STACK_NAME"

          echo ""

          echo "AWS Region:"
          echo "$AWS_REGION"

          echo ""

          echo "Template:"
          echo "$TEMPLATE_FILE"

          echo ""

          echo "Template Bucket:"
          echo "$TEMPLATE_BUCKET"

          echo ""

          echo "Deployment completed successfully."

          echo "======================================"